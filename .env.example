# Environment Variables for ExpenseNavigator

# Session Configuration
SESSION_SECRET=your-super-secret-session-key-here

# Storage Backend Configuration
# Set to 'true' to use Supabase, 'false' for PostgreSQL
USE_SUPABASE=false

# Supabase Configuration (only needed if USE_SUPABASE=true)
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
SUPABASE_ANON_KEY=your-anon-key-here

# Database Configuration (PostgreSQL - used by the app)
# The app reads DB_USER/DB_PASSWORD/DB_HOST/DB_NAME/DB_PORT from process.env
DB_USER=expense_user
DB_PASSWORD=yourpassword
DB_HOST=localhost
DB_NAME=expense_app
DB_PORT=5432

# Optional DATABASE_URL (alternative to individual DB_* vars)
# DATABASE_URL=postgresql://username:password@localhost:5432/expense_app

# Development Settings
NODE_ENV=development
PORT=5000

# DO NOT COMMIT real secrets. Copy this file to .env and fill values before running locally.
